# ──────────────────────────────────────────────────────────
# Smart Home Pro — Environment Configuration
# Copy to .env and adjust values
# ──────────────────────────────────────────────────────────

# ── General ──
TZ=Europe/Stockholm
LOG_LEVEL=info

# ── Homey Backend ──
LATITUDE=59.3293
LONGITUDE=18.0686

# ── Dashboard ──
HOMEY_TOKEN=

# ── Security ──
# Comma-separated list of allowed CORS origins (leave empty for default localhost)
ALLOWED_ORIGINS=

# ── Nginx ──
NGINX_PORT=80

# ── Monitoring ──
GRAFANA_PASSWORD=changeme

# ── Docker Resource Overrides ──
# PORT=3000
